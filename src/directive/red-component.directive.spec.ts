import { RedComponentDirective } from './red-component.directive';

describe('RedComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new RedComponentDirective();
    expect(directive).toBeTruthy();
  });
});
